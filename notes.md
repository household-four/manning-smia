# How To

## Build
`cd` to the directory containing the pom.xml, then run: 

`mvn clean verify`

## Run/Test
`cd` to the directory containing the pom.xml, then run: 

`mvn spring-boot:run`

Then navigate to localhost:8080./